import { FormatCaseInputComponent } from './format-case-input.component';

describe('FormatCaseInputComponent', () => {
  let component: FormatCaseInputComponent;

  beforeEach(() => {
    component = new FormatCaseInputComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
